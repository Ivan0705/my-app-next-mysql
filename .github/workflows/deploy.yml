name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Handle API routes for static export
        run: |
          if [ -d "app/api" ]; then
            echo "📁 Temporarily disabling API routes..."
            mv app/api app/_api_temp
          fi
      
      - name: Configure for GitHub Pages
        run: |
          # Копируем продакшн конфиг
          cp next.config.prod.ts next.config.ts
          
          # Устанавливаем production mode
          echo "NODE_ENV=production" >> $GITHUB_ENV
          
          # Создаем .nojekyll
          touch .nojekyll
          
          echo "✅ GitHub Pages configuration ready"
      
      - name: Build Next.js
        env:
          NODE_ENV: production
        run: npm run build
      
      - name: Fix CSS paths in HTML files
        run: |
          echo "🔧 Fixing CSS paths in HTML files..."
          # Исправляем абсолютные пути на относительные
          find out -name "*.html" -exec sed -i \
            's|href="/_next/|href="./_next/|g; \
             s|src="/_next/|src="./_next/|g; \
             s|href="/static/|href="./static/|g; \
             s|src="/static/|src="./static/|g' {} \;
          echo "✅ HTML paths fixed"
      
      - name: Fix asset paths in CSS files
        run: |
          echo "🔧 Fixing asset paths in CSS files..."
          # Исправляем url() в CSS
          find out -name "*.css" -exec sed -i \
            's|url(/|url(./|g; \
             s|url("/|url("./|g; \
             s|url(\'/|url(\'./|g' {} \; 2>/dev/null || true
          echo "✅ CSS paths fixed"
      
      - name: Verify fixed paths
        run: |
          echo "🔍 Verifying fixed paths..."
          
          # Проверяем HTML
          if grep -q 'href="./_next/' out/index.html 2>/dev/null; then
            echo "✅ HTML has relative paths"
          else
            echo "❌ HTML still has absolute paths"
            echo "Sample:"
            grep -o 'href="[^"]*\.css"' out/index.html | head -2
          fi
          
          # Проверяем CSS файлы
          if [ -d "out/_next/static/css" ]; then
            CSS_COUNT=$(ls out/_next/static/css/*.css 2>/dev/null | wc -l)
            echo "✅ Found $CSS_COUNT CSS files"
          fi
      
      - name: Create .nojekyll file
        run: |
          # Важно для Next.js на GitHub Pages
          touch out/.nojekyll
          echo "📄 Created .nojekyll file in output"
      
      - name: Restore API routes
        if: always()
        run: |
          if [ -d "app/_api_temp" ]; then
            mv app/_api_temp app/api
          fi
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      
      - name: Show deployment info
        run: |
          echo "🚀 Deployment successful!"
          echo "🌐 Your site: https://ivan0705.github.io/my-app-next-mysql"
          echo "⏰ It may take 1-2 minutes to propagate"
